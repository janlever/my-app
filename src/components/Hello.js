import React from "react"

// const Hello = ({ name }) => {
//   return <div>Hello {name}</div>
// }
const Hello = ({ name, age }) => (
  <div>
    Hello {name} {age}
  </div>
)

export default Hello
